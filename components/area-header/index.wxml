<!--components/area-header/index.wxml-->
<view class="header">
  <view class="title">{{title}}</view>
  <view wx:if="{{showRight}}">
    <view class="slot"><slot></slot></view>
    <view class="default">
      <text>{{rightText}}</text>
      <image src="/assets/images/icons/arrow-right.png" class="icon" />
    </view>
  </view>
</view>
